define([],
		function( ){

			function DataParser(){


				return( this );

			}

			var p = DataParser.prototype;

			p.init = function() {

			}

			// Return the base class constructor.
			return( DataParser );
		}
);